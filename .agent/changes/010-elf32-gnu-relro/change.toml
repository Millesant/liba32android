schema = 1
id = "010-elf32-gnu-relro"
kind = "feature"
size = "S2"
status = "implemented"
workflow = "feature"
control_revision = "1df73390ad04c0909e3ca42daa690789f150f1ca"
base_revision = "fd6670f3d40d51ad6fa5995b27ede87e4aa1b51c"
acceptance = [
  "PT_GNU_RELRO ranges are validated by the shared ELF32 load plan and exposed as biased guest-only loader metadata",
  "loader mapping does not seal RELRO early; relocation writes remain possible until the explicit sealing call",
  "RELRO page rounding follows host-page mprotect granularity and every protected page is already mapped and readable",
  "sealing is caller-bounded, preflights the complete range set before mutation, and never broadens permissions",
  "RELRO sealing is idempotent and reports protection/rollback failures distinctly",
  "the pinned real ARMv7 fixture exposes its observed GNU_RELRO range, completes existing relocations, then becomes read-only without changing relocated bytes",
  "lazy binding, DT_PLTGOT, RELRO sharing, version-aware/process-wide interposition, TLS, IFUNC, broader relocations, and guest execution remain outside scope",
  "existing loader/dependency/symbol/relocation APIs remain source-compatible apart from additive RELRO metadata/API",
  "final exact-head Linux A32 smoke, Android x86_64 probe, and Android arm64-v8a cross-build pass"
]

[convergence]
requirements = "aligned"
specs = "aligned"
implementation = "aligned"
tests = "aligned"
documentation = "aligned"
decisions = "not_applicable"
tasks = "pending"
evidence = "pending"
repository = "pending"
