schema = 1
id = "015-elf32-symbol-scope-policy"
kind = "feature"
size = "S2"
status = "active"
workflow = "feature"
control_revision = "2b7c8b9245a64560cc9e986d554d99233e34b8c5"
base_revision = "721cb66d6d58bf13632ba726655d915cfe217b79"
acceptance = [
  "DT_SYMBOLIC and DF_SYMBOLIC are retained as validated linker metadata; both forms may coexist and duplicate singleton forms fail explicitly",
  "reference lookup accepts an explicit ordered global scope while preserving the existing graph-local breadth-first closure",
  "ordinary references search the explicit global scope before local graph scope, while symbolic requesters search themselves first and then continue through global plus local scope without duplicate lookup",
  "scope accounting remains caller-bounded and invalid global object indices fail explicitly",
  "version matching and existing symbol eligibility rules apply identically across self, global, and local candidates",
  "main and PLT relocation resolution inherit the requester/global-scope policy through the existing reference lookup path",
  "the supplied ARM32 libfmod.so and VLC libvlc.so DT_SYMBOLIC/DF_SYMBOLIC evidence is documented without vendoring third-party binaries",
  "exact-head Linux A32 smoke and both Android required checks pass before the feature is closed"
]

[convergence]
requirements = "pending"
specs = "pending"
implementation = "pending"
tests = "pending"
documentation = "pending"
decisions = "not_applicable"
tasks = "pending"
evidence = "pending"
repository = "pending"
