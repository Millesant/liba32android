schema = 1
id = "025-a32-host-service-registry"
kind = "feature"
size = "S2"
status = "implemented"
workflow = "feature"
control_revision = "f4e926e81ad91d13d02a006f4a18a00f66ae0bab"
base_revision = "d69a12dcc07cf3c10b63975a05c0cb13bd91d52c"
acceptance = [
  "a game-agnostic A32HostServiceRegistry composes caller-owned A32HostServiceHandler objects by exact SVC immediate without Dynarmic or platform types",
  "the registry borrows a finite entry span and transfers no ownership or allocation responsibility",
  "no matching SVC returns Unhandled without invoking a child",
  "exactly one matching non-null child receives the unchanged GuestMemory, exact SVC immediate, mutable registers, and CPSR and its disposition is returned unchanged",
  "duplicate matching IDs, a matching null handler, or a direct self-entry return Failed before any child handler is invoked",
  "invalid entries for unrelated service IDs do not poison exact lookup",
  "runtime coverage proves direct lookup behavior plus composition through feature 024 using an ARM SVC program and resume-to-stop execution",
  "no Android API constants, ABI argument decoding, guest-string decoding, compatibility shim ELF generation, symbol policy, syscall semantics, or namespace/search policy is introduced",
  "exact-head Linux A32 smoke and both required Android checks pass before the feature is closed"
]

[convergence]
requirements = "aligned"
specs = "aligned"
implementation = "aligned"
tests = "aligned"
documentation = "aligned"
decisions = "not_applicable"
tasks = "aligned"
evidence = "pending"
repository = "aligned"
