schema = 1
change = "025-a32-host-service-registry"

[[tasks]]
id = "T001"
status = "implemented"
depends_on = []
acceptance = "Add the caller-owned exact-SVC registry contract above A32HostServiceHandler without platform or Dynarmic types."

[[tasks]]
id = "T002"
status = "implemented"
depends_on = ["T001"]
acceptance = "Implement exact lookup with Unhandled for no match and pre-dispatch Failed behavior for duplicate, matching-null, or direct-self entries."

[[tasks]]
id = "T003"
status = "implemented"
depends_on = ["T002"]
acceptance = "Add direct registry regressions plus an ARM SVC integration path through execute_a32_with_services, and register source/test targets."

[[tasks]]
id = "T004"
status = "active"
depends_on = ["T003"]
acceptance = "Run exact-head Linux A32 smoke and both required Android checks, then reconcile status/evidence/current state before closing feature 025."
