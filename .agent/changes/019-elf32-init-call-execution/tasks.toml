schema = 1
change = "019-elf32-init-call-execution"

[[tasks]]
id = "T001"
status = "verified"
depends_on = []
acceptance = "Add optional exact stop-PC termination/reporting to cpu::ExecutionRequest/Result without changing behavior when absent."

[[tasks]]
id = "T002"
status = "verified"
depends_on = ["T001"]
acceptance = "Prove ARM and Thumb returns can reach an unmapped stop PC without fetching it, plus initial-stop and budget behavior."

[[tasks]]
id = "T003"
status = "verified"
depends_on = ["T002"]
acceptance = "Add ordered INIT_ARRAY plan execution with ARM/Thumb derivation, caller stack/return-stop options, per-call ceilings, and explicit failure provenance."

[[tasks]]
id = "T004"
status = "verified"
depends_on = ["T003"]
acceptance = "Add lifecycle execution tests for ordered calls, guest side effects, invalid options/functions, exceptions/faults, instruction exhaustion, and stop-on-first-failure semantics."

[[tasks]]
id = "T005"
status = "done"
depends_on = ["T004"]
acceptance = "Reconcile accepted specs/docs/state/evidence and pass exact-head Linux A32 smoke plus both required Android checks."
