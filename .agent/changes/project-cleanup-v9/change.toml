schema = 1
id = "project-cleanup-v9"
kind = "maintenance"
size = "S2"
status = "active"
workflow = "feature"
control_revision = "f4e926e81ad91d13d02a006f4a18a00f66ae0bab"
base_revision = "f45da9d530524a2701b053e84c36f5860e607582"
acceptance = [
  "canonical STATE/NEXT reflect the verified feature-024 closure, current control revision, runtime/service/provider capabilities, and only genuine deferred work",
  "current architecture docs no longer describe already-implemented downstream linker/loader/runtime layers as merely future work",
  "layer-local non-goals are explicitly distinguished from repository-wide missing functionality so documentation cannot hide an implementation gap by scope ambiguity",
  "historical root specs remain unrewritten but their index explicitly warns that embedded ACTIVE/NOT RUN statuses are historical snapshots and gives feature 010 as a concrete superseded example",
  "diagnostics distinguish the implemented internal runtime/service layer from the still-missing stable public embedding/error API",
  "repository audit records no TODO/FIXME/placeholder/disabled-test markers and no orphan src/tests .cpp files outside CMake registration at the audited revision",
  "stale current NEXT guidance about already-completed lifecycle/provider work is removed and remaining candidates match accepted STATE deferred gaps",
  "no accepted runtime behavior changes; Linux A32 smoke and both required Android checks pass on the exact cleanup revision"
]

[convergence]
requirements = "not_applicable"
specs = "not_applicable"
implementation = "pending"
tests = "pending"
documentation = "pending"
decisions = "not_applicable"
tasks = "pending"
evidence = "pending"
repository = "pending"
