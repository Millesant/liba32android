schema = 1
change = "017-elf32-lifecycle-array-metadata"

[[tasks]]
id = "T001"
status = "verified"
depends_on = []
acceptance = "Collect/rebase/validate DT_INIT_ARRAY(+SZ) and DT_FINI_ARRAY(+SZ), rejecting duplicate, incomplete, mis-sized, overflowed, or unreadable declarations."

[[tasks]]
id = "T002"
status = "verified"
depends_on = ["T001"]
acceptance = "Add a read-only caller-bounded ELF32 function-array decoder that returns raw logical 32-bit entries in declaration order without filtering sentinel values."

[[tasks]]
id = "T003"
status = "verified"
depends_on = ["T002"]
acceptance = "Add focused synthetic coverage for empty/exact arrays, sentinels, entry ceilings, read failures, no-mutation behavior, and preservation of existing metadata semantics."

[[tasks]]
id = "T004"
status = "done"
depends_on = ["T003"]
acceptance = "Reconcile accepted specs/docs/state/evidence and pass exact-head Linux A32 smoke plus both required Android checks."
