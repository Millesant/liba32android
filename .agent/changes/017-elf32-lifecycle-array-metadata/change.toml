schema = 1
id = "017-elf32-lifecycle-array-metadata"
kind = "feature"
size = "S2"
status = "active"
workflow = "feature"
control_revision = "2b7c8b9245a64560cc9e986d554d99233e34b8c5"
base_revision = "18cfd59214b4099118b49e38813b50a13291c65e"
acceptance = [
  "DT_INIT_ARRAY/DT_INIT_ARRAYSZ and DT_FINI_ARRAY/DT_FINI_ARRAYSZ are retained as validated unique paired metadata",
  "array addresses receive one checked load-bias rebase; sizes must be divisible by the 4-byte ELF32 function-pointer width and declared guest ranges must be readable",
  "validated metadata exposes guest-only init/fini array descriptors without host-pointer conversion or guest mutation",
  "a caller-bounded read-only decoder materializes raw 32-bit array entries in declared order and preserves null and all-ones sentinels for later lifecycle policy",
  "malformed, oversized, overflowed, and unreadable arrays fail explicitly before any lifecycle execution",
  "the supplied VLC ARMv7 lifecycle evidence and Android bionic constructor/destructor ordering semantics are documented without vendoring third-party binaries",
  "DT_INIT/DT_FINI, DT_PREINIT_ARRAY, dependency-order lifecycle planning, constructor/destructor execution, recursion guards, and unload remain outside this feature",
  "exact-head Linux A32 smoke and both required Android checks pass before the feature is closed"
]

[convergence]
requirements = "pending"
specs = "pending"
implementation = "pending"
tests = "pending"
documentation = "pending"
decisions = "not_applicable"
tasks = "pending"
evidence = "pending"
repository = "pending"
