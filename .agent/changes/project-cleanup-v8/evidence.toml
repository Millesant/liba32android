schema = 1
change = "project-cleanup-v8"

[[evidence]]
id = "persisted-layout-inspection"
kind = "inspection"
status = "passed"
claim = "The repository cleanup persisted the intended ownership layout without dropping moved files or renaming test identities."
source = "Persisted-tree inspection and CMake identity comparison at 5b1cf991272632ed44d6276d6ec5e982ef732f28"
result = "All required new source/test/tool paths are present and sampled old paths are absent. The runtime still registers 14 source translation units. Test registration preserves exactly 30 executable targets and 49 CTest names with no missing or extra identities."
limitations = "Structural inspection is not build/runtime validation; exact-head checks validate the integrated tree separately."

[[evidence]]
id = "exact-head-ci"
kind = "validation"
status = "passed"
claim = "The complete project cleanup preserves the required build/test matrix."
source = "GitHub exact-head checks at 5b1cf991272632ed44d6276d6ec5e982ef732f28: Linux A32 smoke 107823867033; Android arm64-v8a cross-build 107823867078; Android x86_64 address-space probe 107823866703"
result = "All three required checks completed successfully on the exact implementation revision."
limitations = "These checks validate repository build/test integration for the cleanup; they establish no new runtime compatibility claim."
