schema = 1
id = "016-elf32-link-map-global-group"
kind = "feature"
size = "S2"
status = "done"
workflow = "feature"
control_revision = "2b7c8b9245a64560cc9e986d554d99233e34b8c5"
base_revision = "d995ee5b1ae591f448546a7588078ab0d4433e9e"
result_revision = "0c374ff84990ee3d64c06a1037f846d90054e5e4"
acceptance = [
  "DT_FLAGS_1 is retained as validated linker metadata and DF_1_GLOBAL membership is explicit without changing existing DT_FLAGS/DF_SYMBOLIC behavior",
  "a caller-owned ELF32 link map preserves loaded-object and mapping lifetime across multiple bounded root-load operations",
  "existing object identities are reused across root loads and identity/image mismatches fail explicitly instead of remapping aliases",
  "a failed append rolls back only mappings, objects, edges, roots, and global-membership state introduced by that append while preserving pre-existing link-map state",
  "global scope ordering is stable, deduplicated, and includes caller-designated global roots plus objects carrying DF_1_GLOBAL",
  "the accumulated graph remains consumable by existing feature-015 reference lookup through ordinary object indices and a caller-owned global-scope span",
  "the existing one-shot dependency-graph loader remains source-compatible and behaviorally unchanged",
  "filesystem/search-path selection, Android namespace accessibility, LD_PRELOAD parsing, RTLD flags, unload/dlopen/dlsym, and compatibility shims remain outside this feature",
  "exact-head Linux A32 smoke and both required Android checks pass before the feature is closed"
]

[convergence]
requirements = "aligned"
specs = "aligned"
implementation = "aligned"
tests = "aligned"
documentation = "aligned"
decisions = "not_applicable"
tasks = "aligned"
evidence = "aligned"
repository = "aligned"
