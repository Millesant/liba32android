schema = 1
change = "016-elf32-link-map-global-group"

[[tasks]]
id = "T001"
status = "verified"
depends_on = []
acceptance = "Retain DT_FLAGS_1 as validated singleton linker metadata and expose DF_1_GLOBAL with duplicate/non-global regression coverage."

[[tasks]]
id = "T002"
status = "verified"
depends_on = ["T001"]
acceptance = "Define a caller-owned persistent link-map contract with stable accumulated graph indexes, root records, ordered global-scope indexes, and explicit local/global root policy."

[[tasks]]
id = "T003"
status = "verified"
depends_on = ["T002"]
acceptance = "Add transactional append loading that reuses existing identities, rejects cross-load identity/image mismatches, and preserves pre-existing link-map state on failure."

[[tasks]]
id = "T004"
status = "verified"
depends_on = ["T003"]
acceptance = "Populate stable deduplicated global membership from caller-designated global roots plus DF_1_GLOBAL objects and prove direct feature-015 lookup consumption."

[[tasks]]
id = "T005"
status = "verified"
depends_on = ["T004"]
acceptance = "Preserve the existing one-shot dependency-graph loader behavior and add focused multi-root, reuse, rollback, limits, and ordering tests."

[[tasks]]
id = "T006"
status = "done"
depends_on = ["T005"]
acceptance = "Reconcile accepted specs/docs/state/evidence and pass exact-head Linux A32 smoke plus both required Android checks."
