schema = 1
id = "009-elf32-jump-slot-relocations"
kind = "feature"
size = "S2"
status = "active"
workflow = "feature"
control_revision = "9240ab19507b86491398a5c9fdf0deb58e2fdc91"
base_revision = "bbad7db39e430ba87a373841e3ede954696c85c5"
acceptance = [
  "validated PLT REL metadata is consumed without re-parsing raw dynamic tags",
  "PLT planning accepts only bounded R_ARM_JUMP_SLOT entries and remains read-only",
  "JUMP_SLOT references reuse the existing bounded graph-local symbol policy",
  "eager JUMP_SLOT application writes the resolved logical guest symbol value and ignores the in-place REL word as an addend",
  "all semantic checks finish before the first PLT write and late failures roll back prior PLT writes",
  "no lazy binding, DT_PLTGOT resolver protocol, version-aware interposition, IFUNC, TLS, RELRO, or permission broadening is introduced",
  "a reproducible real ARMv7 consumer/provider fixture pair contains an observed DT_NEEDED plus R_ARM_JUMP_SLOT and applies it through the loaded dependency graph",
  "existing main-DT_REL relocation behavior remains source-compatible and green",
  "final exact-head Linux A32 smoke, Android x86_64 probe, and Android arm64-v8a cross-build pass"
]

[convergence]
requirements = "aligned"
specs = "aligned"
implementation = "aligned"
tests = "aligned"
documentation = "aligned"
decisions = "not_applicable"
tasks = "aligned"
evidence = "aligned"
repository = "aligned"
