schema = 1
change = "009-elf32-jump-slot-relocations"

[[tasks]]
id = "T001"
status = "verified"
depends_on = []
acceptance = "Add bounded read-only PLT REL planning and reference resolution for R_ARM_JUMP_SLOT while preserving existing main REL APIs."

[[tasks]]
id = "T002"
status = "verified"
depends_on = ["T001"]
acceptance = "Apply eager JUMP_SLOT writes transactionally as S, ignoring the in-place word as an addend, with rollback coverage."

[[tasks]]
id = "T003"
status = "implemented"
depends_on = ["T002"]
acceptance = "Build a reproducible ARMv7 consumer/provider fixture pair with real DT_NEEDED + R_ARM_JUMP_SLOT and validate graph-backed application."

[[tasks]]
id = "T004"
status = "planned"
depends_on = ["T003"]
acceptance = "Converge specs/docs/state/evidence and pass the final exact-head Linux and Android CI gate."
