schema = 1
change = "elf32-header-layering-v7"

[[tasks]]
id = "T001"
status = "done"
depends_on = []
acceptance = "Extract the shared ELF32 load error contract without changing enum ordering, formatter identity, or loader API exposure."

[[tasks]]
id = "T002"
status = "done"
depends_on = ["T001"]
acceptance = "Remove unnecessary loader-header dependencies from load-plan and dynamic-placement headers and verify the full build/test matrix."
