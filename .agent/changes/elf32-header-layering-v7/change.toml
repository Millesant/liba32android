schema = 1
id = "elf32-header-layering-v7"
kind = "maintenance"
size = "S1"
status = "active"
workflow = "feature"
control_revision = "1df73390ad04c0909e3ca42daa690789f150f1ca"
base_revision = "0b51069def55cfe3ce35621845d948ea2401a38f"
acceptance = [
  "Elf32LoadError and its formatter declaration live in one focused shared header with identical enumerator order and values",
  "elf32_loader.h remains source-compatible for existing callers by including the shared load-error header",
  "elf32_load_plan.h no longer includes elf32_loader.h and includes only the load-error and guest-memory contracts it uses",
  "elf32_dynamic_placement.h no longer includes elf32_loader.h and includes only the load-error and guest-memory contracts it uses",
  "loader/load-plan/placement behavior and public function signatures remain unchanged",
  "Linux 49-test suite and both Android CI lanes pass on the exact resulting revision"
]

[convergence]
requirements = "not_applicable"
specs = "not_applicable"
implementation = "pending"
tests = "pending"
documentation = "not_applicable"
decisions = "not_applicable"
tasks = "aligned"
evidence = "pending"
repository = "pending"
