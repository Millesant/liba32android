schema = 1
change = "024-a32-host-service-dispatch"

[[evidence]]
id = "exact-head-ci"
kind = "validation"
status = "passed"
claim = "Bounded A32 host-service dispatch passes the required exact-head matrix."
source = "GitHub commit checks at d4e7b480e28d13e8edc5dd1ccf28abbc3072a7a1"
result = "Linux A32 smoke check 108377362582, Android x86_64 address-space probe check 108377362552, and Android arm64-v8a cross-build check 108377362391 all completed successfully."
limitations = "This validates the game-agnostic dispatcher only. Android service registries, ABI marshalling, shim ELF generation, libc/JNI/graphics/audio implementations, syscall semantics, and namespace/search policy remain outside feature 024."

[[evidence]]
id = "service-dispatch-regressions"
kind = "test"
status = "passed"
claim = "Runtime regressions cover ARM/Thumb resume, exact IDs, host side effects, limits, handler failures, CPU failures, stop-PC exhaustion, and fixed-budget compatibility."
source = "tests/runtime/a32_service_dispatch.cpp at d4e7b480e28d13e8edc5dd1ccf28abbc3072a7a1"
result = "The exact-head Linux A32 smoke lane completed successfully with a32_host_service_dispatch registered in-tree."
limitations = "Synthetic SVC programs prove the dispatcher contract, not any Android API implementation."
