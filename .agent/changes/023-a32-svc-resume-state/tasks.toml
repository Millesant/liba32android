schema = 1
change = "023-a32-svc-resume-state"

[[tasks]]
id = "T001"
status = "active"
depends_on = []
acceptance = "Add optional initial-CPSR request state and exact SVC-immediate result reporting while preserving existing default execution behavior."

[[tasks]]
id = "T002"
status = "pending"
depends_on = ["T001"]
acceptance = "Capture the Dynarmic SVC immediate without exposing engine types and retain existing exception termination semantics."

[[tasks]]
id = "T003"
status = "pending"
depends_on = ["T002"]
acceptance = "Add ARM and Thumb tests for exact immediates, post-SVC PC/register/CPSR state, and continuation from returned state."

[[tasks]]
id = "T004"
status = "pending"
depends_on = ["T003"]
acceptance = "Reconcile runtime/CPU docs, state/evidence, and pass exact-head Linux A32 smoke plus both required Android checks."
