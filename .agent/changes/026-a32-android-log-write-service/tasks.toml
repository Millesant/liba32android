schema = 1
change = "026-a32-android-log-write-service"

[[tasks]]
id = "T001"
status = "verified"
depends_on = []
acceptance = "Capture authoritative AOSP __android_log_write and AAPCS32 register/result evidence plus the supplied-artifact motivation."

[[tasks]]
id = "T002"
status = "verified"
depends_on = ["T001"]
acceptance = "Add the src/compat sink/service contract with caller-selected exact SVC ID, r0-r2 ABI mapping, and signed r0 result propagation."

[[tasks]]
id = "T003"
status = "verified"
depends_on = ["T002"]
acceptance = "Implement bounded GuestMemory C-string copies, null-tag preservation, null-text/read/overflow/unterminated pre-sink failures, and no direct host liblog dependency."

[[tasks]]
id = "T004"
status = "verified"
depends_on = ["T003"]
acceptance = "Add compatibility regressions for registry/dispatcher composition, signed result bits, null tag, exact limits, mismatch, null text, unreadable and unterminated failures; register the compatibility test domain."

[[tasks]]
id = "T005"
status = "verified"
depends_on = ["T004"]
acceptance = "Run exact-head Linux A32 smoke and both required Android checks, then reconcile spec/status/evidence/current state before closing feature 026."
