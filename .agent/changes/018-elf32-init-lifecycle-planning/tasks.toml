schema = 1
change = "018-elf32-init-lifecycle-planning"

[[tasks]]
id = "T001"
status = "active"
depends_on = []
acceptance = "Define root-scoped lifecycle plan/result/error types with explicit object and total-entry ceilings."

[[tasks]]
id = "T002"
status = "pending"
depends_on = ["T001"]
acceptance = "Implement dependency-first DFS planning with cycle/shared-dependency suppression and invalid-edge/object-limit failure."

[[tasks]]
id = "T003"
status = "pending"
depends_on = ["T002"]
acceptance = "Decode each reachable object's INIT_ARRAY under the shared total-entry budget, filter 0/0xffffffff sentinels, and retain object/entry/function provenance."

[[tasks]]
id = "T004"
status = "pending"
depends_on = ["T003"]
acceptance = "Add focused coverage for dependency order, cycles, shared dependencies, sentinels, invalid roots/edges, ceilings, decoder errors, and no mutation."

[[tasks]]
id = "T005"
status = "pending"
depends_on = ["T004"]
acceptance = "Reconcile accepted specs/docs/state/evidence and pass exact-head Linux A32 smoke plus both required Android checks."
