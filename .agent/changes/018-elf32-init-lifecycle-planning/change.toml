schema = 1
id = "018-elf32-init-lifecycle-planning"
kind = "feature"
size = "S2"
status = "active"
workflow = "feature"
control_revision = "f4e926e81ad91d13d02a006f4a18a00f66ae0bab"
base_revision = "dfb6c22e30245df71c245cfe19a0ceabfb6e6d55"
acceptance = [
  "a root-scoped read-only INIT_ARRAY planner walks dependency edges before the requester object in deterministic edge order",
  "cycles and shared dependencies are suppressed so each reachable object contributes constructors at most once",
  "invalid root/edge indices and caller object/entry ceilings fail explicitly with no successful partial plan",
  "INIT_ARRAY entries are decoded through the feature-017 bounded raw decoder and null/all-ones sentinels are omitted from the executable-call plan",
  "planned calls retain defining object index, array entry index, and raw logical 32-bit function value including any Thumb discriminator bit",
  "planning does not mutate guest memory, mappings, dependency graph state, or constructor-called state",
  "legacy DT_INIT, DT_PREINIT_ARRAY, FINI_ARRAY/destructor planning, guest CPU invocation, recursion state persisted across calls, dlopen lifecycle, and unload remain outside this feature",
  "exact-head Linux A32 smoke and both required Android checks pass before the feature is closed"
]

[convergence]
requirements = "pending"
specs = "pending"
implementation = "pending"
tests = "pending"
documentation = "pending"
decisions = "not_applicable"
tasks = "pending"
evidence = "pending"
repository = "pending"
