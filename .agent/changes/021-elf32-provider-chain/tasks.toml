schema = 1
change = "021-elf32-provider-chain"

[[tasks]]
id = "T001"
status = "active"
depends_on = []
acceptance = "Add the borrowed ordered Elf32DependencyProviderChain API and document provider/list lifetime."

[[tasks]]
id = "T002"
status = "pending"
depends_on = ["T001"]
acceptance = "Implement strict ordered lookup with NotFound-only fallback, hard-failure/success short-circuit, and exact requester/request/limit forwarding."

[[tasks]]
id = "T003"
status = "pending"
depends_on = ["T002"]
acceptance = "Add focused resolver tests for empty/null chains, fallback order, hard failure, success short-circuit, requester bytes, legacy child providers, and unchanged validation."

[[tasks]]
id = "T004"
status = "pending"
depends_on = ["T003"]
acceptance = "Reconcile accepted specs/docs/state/evidence and pass exact-head Linux A32 smoke plus both required Android checks."
