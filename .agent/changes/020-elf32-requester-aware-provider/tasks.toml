schema = 1
change = "020-elf32-requester-aware-provider"

[[tasks]]
id = "T001"
status = "active"
depends_on = []
acceptance = "Add the requester-aware provider hook plus borrowed requester identity resolver option while preserving the legacy provider path."

[[tasks]]
id = "T002"
status = "pending"
depends_on = ["T001"]
acceptance = "Route resolver calls through the requester-aware hook without changing limits, ownership, ordered occurrences, or error translation."

[[tasks]]
id = "T003"
status = "pending"
depends_on = ["T002"]
acceptance = "Have recursive one-shot and persistent link-map loading supply the exact currently processed object identity for direct and nested requests."

[[tasks]]
id = "T004"
status = "pending"
depends_on = ["T003"]
acceptance = "Add focused tests for legacy fallback, exact requester bytes, nested requester propagation, repeated requests, and unchanged failures/ceilings."

[[tasks]]
id = "T005"
status = "pending"
depends_on = ["T004"]
acceptance = "Reconcile accepted specs/docs/state/evidence and pass exact-head Linux A32 smoke plus both required Android checks."
