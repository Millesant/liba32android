schema = 1
id = "011-elf32-combined-relocation-transaction"
kind = "feature"
size = "S2"
status = "implemented"
workflow = "feature"
control_revision = "0c8f0e26c9227599eb8bfae48106b53074a24188"
base_revision = "58e69cc7a5f40dc7ae7917f7f4435bad5145a8fc"
acceptance = [
  "one additive API atomically covers the supported main DT_REL and eager PLT REL tables for one loaded object",
  "both tables complete planning, symbol resolution, validation, and final-word calculation before the first guest-memory write",
  "combined writes execute in deterministic main-table order followed by PLT-table order",
  "cross-table duplicate write targets fail before mutation",
  "a later PLT write failure rolls back earlier main writes with table-qualified failure identity",
  "existing independent main and PLT application APIs retain their behavior",
  "final exact-head Linux A32 smoke, Android x86_64 probe, and Android arm64-v8a cross-build pass"
]
[convergence]
requirements = "aligned"
specs = "aligned"
implementation = "aligned"
tests = "aligned"
documentation = "aligned"
decisions = "not_applicable"
tasks = "aligned"
evidence = "pending"
repository = "pending"
