schema = 1
id = "repository-organization-v7"
kind = "maintenance"
size = "S2"
status = "done"
workflow = "feature"
control_revision = "1df73390ad04c0909e3ca42daa690789f150f1ca"
base_revision = "657fabba9ce42ac574533f384ac830caceed668d"
result_revision = "f24c87026b66838c0742bf61ebafe27fba2f117e"
acceptance = [
  "project metadata declares the v7 state/spec/change/action directories and bleeding integration branch",
  "accepted current project contracts live under .agent/specs while legacy numbered feature packages remain explicitly historical",
  "repository instructions and continuation state use stable change identity for substantial work",
  "the root CMake file becomes orchestration-only while dependency/runtime/probe/test declarations retain their existing semantics, targets, test names, and artifact paths",
  "duplicated ELF32 little-endian decoding and checked guest-address addition are centralized in one private helper without public API or semantic changes",
  "feature 010 RELRO work remains intact and is validated together with the cleanup on the resulting exact revision",
  "Linux CTest, Android x86_64 probe, and Android arm64-v8a cross-build pass on the exact persisted revision"
]

[convergence]
requirements = "not_applicable"
specs = "aligned"
implementation = "aligned"
tests = "aligned"
documentation = "aligned"
decisions = "not_applicable"
tasks = "aligned"
evidence = "aligned"
repository = "aligned"
