schema = 1
change = "repository-organization-v7"

[[evidence]]
id = "persisted-structure-inspection"
kind = "inspection"
status = "passed"
claim = "The CMake reorganization moves the existing dependency/runtime/probe/test declarations without changing their content, and the ELF helper cleanup removes the duplicated local decode/address-add definitions."
source = "Persisted-tree inspection at ff1792457f05bd9dd58740e1b768576b9ad4f1c3"
result = "Each of the four new CMake modules matched its corresponding pre-refactor CMakeLists.txt section byte-for-byte after section extraction; all four modified ELF translation units include elf32_bytes.h with no remaining local checked-add or little-endian decoder definition."
limitations = "This is structural inspection, not execution evidence. Compile/test behavior is validated separately by CI #234."

[[evidence]]
id = "implementation-exact-head-ci"
kind = "validation"
status = "passed"
claim = "The v7 workflow/build organization and private ELF helper refactor preserve the repository build/test matrix on the exact implementation revision."
source = "GitHub Actions CI #234 / run 35946857448"
result = "At ff1792457f05bd9dd58740e1b768576b9ad4f1c3, Linux configured/built and passed 49/49 CTest; Android x86_64 address-space probe passed; Android arm64-v8a runtime/diagnostics configured and built successfully."
limitations = "The subsequent documentation/state convergence revision requires its own exact-head gate before the maintenance change is done."
