schema = 1
change = "repository-organization-v7"

[[tasks]]
id = "T001"
status = "done"
depends_on = []
acceptance = "Adopt v7 project metadata/current-spec layout and mark the old numbered specs tree as historical."

[[tasks]]
id = "T002"
status = "verified"
depends_on = ["T001"]
acceptance = "Split root CMake declarations into focused dependency/runtime/Android-probe/test modules without changing targets, tests, or artifact locations."

[[tasks]]
id = "T003"
status = "verified"
depends_on = ["T002"]
acceptance = "Centralize duplicated ELF32 byte-decoding and checked guest-address helpers without public API or behavior changes."

[[tasks]]
id = "T004"
status = "done"
depends_on = ["T003"]
acceptance = "Reconcile repository docs/state and pass exact-head Linux plus both Android CI lanes."
