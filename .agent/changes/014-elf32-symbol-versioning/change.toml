schema = 1
id = "014-elf32-symbol-versioning"
kind = "feature"
size = "S2"
status = "implemented"
workflow = "feature"
control_revision = "2b7c8b9245a64560cc9e986d554d99233e34b8c5"
base_revision = "19ed37fe2347b31d2842f25ebfd69fc8a1bda735"
acceptance = [
  "DT_VERSYM, DT_VERDEF/DT_VERDEFNUM, and DT_VERNEED/DT_VERNEEDNUM are retained as validated guest-only linker metadata",
  "version records are traversed through GuestMemory with explicit string and record ceilings and malformed chains fail explicitly",
  "relocation references derive indices 0/1 as unversioned and higher indices through VERNEED or requester VERDEF metadata",
  "provider matching follows Android/bionic semantics: unversioned requests reject hidden definitions, explicit requests match VERDEF name/hash and otherwise fall back to global version index 1",
  "VERNEED library names must correspond to direct dependency SONAMEs before their version records are accepted",
  "existing graph-local breadth-first symbol scope is preserved; DT_SYMBOLIC, global groups, namespaces, and process-wide interposition remain deferred",
  "a reproducible freestanding ARM32 two-DSO fixture proves a LIBC-versioned JUMP_SLOT reference resolves and applies through the real loader graph",
  "final exact-head Linux A32 smoke and both Android required checks pass"
]
[convergence]
requirements = "aligned"
specs = "pending"
implementation = "aligned"
tests = "aligned"
documentation = "aligned"
decisions = "not_applicable"
tasks = "aligned"
evidence = "pending"
repository = "pending"
