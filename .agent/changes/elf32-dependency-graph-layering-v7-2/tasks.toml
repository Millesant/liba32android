schema = 1
change = "elf32-dependency-graph-layering-v7-2"

[[tasks]]
id = "T001"
status = "implemented"
depends_on = []
acceptance = "Extract the dependency graph data model from elf32_dependency_loader.h and make symbol lookup and relocation depend on the graph contract directly."

[[tasks]]
id = "T002"
status = "planned"
depends_on = ["T001"]
acceptance = "Inspect the persisted dependency layering, pass exact-head Linux and Android CI, and converge project state/evidence."
