schema = 1
id = "test-infrastructure-cleanup-v7"
kind = "maintenance"
size = "S1"
status = "active"
workflow = "feature"
control_revision = "1df73390ad04c0909e3ca42daa690789f150f1ca"
base_revision = "38304a2e4fcb3bdb96cd77785256afe135c8653a"
acceptance = [
  "real-fixture tests share one bounded binary-file loader instead of twelve local copies",
  "real-fixture tests that read guest 32-bit words share one byte-explicit GuestMemory helper instead of four local copies",
  "test support remains under tests/ and does not become a runtime/public API dependency",
  "CMake test executable setup uses one small helper while preserving every existing executable target, CTest name, fixture argument, and artifact path",
  "runtime source/behavior and accepted ELF/linker contracts remain unchanged",
  "Linux 49-test suite and both Android CI lanes pass on the exact resulting revision"
]

[convergence]
requirements = "not_applicable"
specs = "not_applicable"
implementation = "pending"
tests = "pending"
documentation = "not_applicable"
decisions = "not_applicable"
tasks = "aligned"
evidence = "pending"
repository = "pending"
