schema = 1
change = "test-infrastructure-cleanup-v7"

[[evidence]]
id = "persisted-structure-equivalence"
kind = "inspection"
status = "passed"
claim = "The test infrastructure cleanup preserves target/test identity while removing duplicated fixture helpers."
source = "Persisted-tree inspection at 56df3ec5a97add0b96f25e02183bdcfbe2aac1c9"
result = "All 30 pre-cleanup test executable target names match exactly; all 49 CTest names match exactly. All 12 real-fixture tests use tests/support/fixture_io.h for binary loading, only the four intended guest-memory tests use the shared read_u32_le helper, and elf32_real_fixture keeps its distinct span-based ELF parser."
limitations = "Inspection establishes structural equivalence; execution behavior is validated separately by CI #238."

[[evidence]]
id = "exact-head-ci"
kind = "validation"
status = "passed"
claim = "The shared test helpers and CMake test-target deduplication preserve the full repository validation matrix."
source = "GitHub Actions CI #238 / run 35979816632"
result = "At 56df3ec5a97add0b96f25e02183bdcfbe2aac1c9, Linux A32 smoke passed 49/49 CTest including elf32_real_relro_seal; Android x86_64 address-space probe and Android arm64-v8a cross-build passed."
limitations = "This is test/build infrastructure cleanup only and establishes no new runtime compatibility behavior."
