schema = 1
id = "project-source-test-layout-v8"
kind = "maintenance"
size = "S2"
status = "implemented"
workflow = "feature"
control_revision = "609e6cb9cff9d00e241aa5437d9904fc7492f407"
base_revision = "ed8f4d97a521aadb27d29089351737e873b4f5b0"
acceptance = [
  "ELF implementation files are grouped by loading, metadata, linking, hardening, and private helper ownership while interface headers remain under src/elf",
  "CPU, memory, ELF unit, ELF integration, fixture, and support tests live in explicit subsystem directories",
  "CMake test registration is split by CPU, memory, and ELF while preserving existing target and CTest identities",
  "runtime behavior and accepted ELF contracts are unchanged"
]

[convergence]
requirements = "not_applicable"
specs = "not_applicable"
implementation = "aligned"
tests = "pending"
documentation = "aligned"
decisions = "not_applicable"
tasks = "pending"
evidence = "pending"
repository = "pending"
