schema = 1
change = "elf32-load-contract-layering-v7-2"

[[evidence]]
id = "planned-tree-inspection"
kind = "inspection"
status = "passed"
claim = "The bounded refactor changes declaration ownership and include dependencies only."
source = "Pre-write structural audit against 65a80b9bc002e92d6eb04b8526b0782c2ca93d2d"
result = "Elf32LoadedSegment, Elf32DynamicSegment, Elf32RelroSegment, and Elf32LoadResult are moved verbatim from elf32_loader.h to elf32_load_types.h; elf32_loader.h re-exports that header; dynamic/RELRO/dependency public headers consume the focused types; elf32_dependency_loader.cpp explicitly includes elf32_loader.h for load_elf32 and Elf32LoadOptions."
limitations = "This establishes the intended declaration/include delta only; compilation and runtime behavior require exact-head CI."

[[evidence]]
id = "exact-head-ci"
kind = "validation"
status = "not_run"
claim = "The load-contract layering cleanup preserves the full repository build/test matrix."
source = "Exact-head GitHub Actions validation of the implementation revision"
result = ""
limitations = "Not run at implementation persistence time."
