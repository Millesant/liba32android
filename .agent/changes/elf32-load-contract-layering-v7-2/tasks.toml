schema = 1
change = "elf32-load-contract-layering-v7-2"

[[tasks]]
id = "T001"
status = "done"
depends_on = []
acceptance = "Adopt the pinned .gpt v7.2.0 control revision for this bounded maintenance round and record the change identity."

[[tasks]]
id = "T002"
status = "done"
depends_on = ["T001"]
acceptance = "Extract loader-produced ELF32 data contracts into a focused header and remove unnecessary public-header dependencies on elf32_loader.h without changing runtime semantics."

[[tasks]]
id = "T003"
status = "done"
depends_on = ["T002"]
acceptance = "Inspect the persisted tree, pass exact-head Linux and Android CI, and converge project state/evidence."
