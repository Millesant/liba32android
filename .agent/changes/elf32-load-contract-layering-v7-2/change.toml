schema = 1
id = "elf32-load-contract-layering-v7-2"
kind = "maintenance"
size = "S1"
status = "done"
workflow = "feature"
control_revision = "609e6cb9cff9d00e241aa5437d9904fc7492f407"
base_revision = "65a80b9bc002e92d6eb04b8526b0782c2ca93d2d"
result_revision = "36b5dc8ddf9b06654fb075d33106f9eb4a417be4"
acceptance = [
  "project maintenance state is pinned to .gpt v7.2.0 and follows the checks-first exact-head CI retrieval contract",
  "ELF32 loaded-segment, dynamic-segment, RELRO-segment, and load-result data contracts live in one focused load-types header",
  "elf32_loader.h remains source-compatible for existing callers by re-exporting the focused load types while retaining the load options and load_elf32 declaration",
  "dynamic, RELRO, and dependency-loader public headers no longer depend on the full loader call surface; the dependency-loader implementation declares its loader call dependency explicitly",
  "loader, dynamic parsing, dependency loading, and RELRO behavior and public function signatures remain unchanged",
  "Linux A32 smoke and both Android CI lanes pass on the exact implementation revision"
]

[convergence]
requirements = "not_applicable"
specs = "not_applicable"
implementation = "aligned"
tests = "aligned"
documentation = "aligned"
decisions = "not_applicable"
tasks = "aligned"
evidence = "aligned"
repository = "aligned"
