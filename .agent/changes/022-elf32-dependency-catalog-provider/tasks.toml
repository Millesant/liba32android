schema = 1
change = "022-elf32-dependency-catalog-provider"

[[tasks]]
id = "T001"
status = "active"
depends_on = []
acceptance = "Add borrowed catalog entry/provider API with explicit lifetime and exact-byte-match contract."

[[tasks]]
id = "T002"
status = "pending"
depends_on = ["T001"]
acceptance = "Implement unique exact-match lookup with owned result copying, duplicate detection, selected-entry validation, and max-image precheck."

[[tasks]]
id = "T003"
status = "pending"
depends_on = ["T002"]
acceptance = "Add focused tests for exact/raw-byte matching, ownership independence, miss/duplicate/malformed/oversize behavior, requester fallback, and application-to-platform catalog chaining."

[[tasks]]
id = "T004"
status = "pending"
depends_on = ["T003"]
acceptance = "Reconcile accepted specs/docs/state/evidence and pass exact-head Linux A32 smoke plus both required Android checks."
