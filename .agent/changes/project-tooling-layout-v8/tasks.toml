schema = 1
change = "project-tooling-layout-v8"

[[tasks]]
id = "T001"
status = "done"
depends_on = []
acceptance = "Separate Android diagnostics from fixture tooling and update all current integration references."

[[tasks]]
id = "T002"
status = "done"
depends_on = ["T001"]
acceptance = "Verify the child scope on the exact integrated implementation revision and converge evidence/state."
