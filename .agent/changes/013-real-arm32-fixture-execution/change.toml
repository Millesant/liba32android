schema = 1
id = "013-real-arm32-fixture-execution"
kind = "feature"
size = "S2"
status = "done"
workflow = "feature"
control_revision = "2b7c8b9245a64560cc9e986d554d99233e34b8c5"
base_revision = "a433bb4d87c87b67b1b3f7a813c8d5fba2484cfd"
result_revision = "e899822ae507d1d3954e670bef7365b3aa1196d4"
acceptance = [
  "the pinned NDK-built ARM32 fixture is loaded through the dependency graph, symbol-resolved, relocated, RELRO-sealed, and executed through the generic A32 CPU adapter on the Linux validation host",
  "fixture_add receives AAPCS r0/r1 arguments and returns the expected value after reading relocated fixture_data and initialized fixture_bss",
  "the execution harness derives ARM versus Thumb state from the defining STT_FUNC symbol and returns through a mapped same-state sentinel without exposing host pointers",
  "execution uses a bounded instruction budget and a bounded mapped guest stack/sentinel region",
  "the fixture remains zero-dependency and no compatibility-layer or Android API emulation is introduced",
  "this feature proves integrated host execution only and does not claim execution on an Android device or 16 KiB AArch64 Android host",
  "final exact-head Linux A32 smoke and both Android required build/probe checks pass"
]
[convergence]
requirements = "aligned"
specs = "aligned"
implementation = "aligned"
tests = "aligned"
documentation = "aligned"
decisions = "not_applicable"
tasks = "aligned"
evidence = "aligned"
repository = "aligned"
