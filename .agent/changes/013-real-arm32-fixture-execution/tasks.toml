schema = 1
change = "013-real-arm32-fixture-execution"

[[tasks]]
id = "T001"
status = "implemented"
depends_on = []
acceptance = "Add a real-fixture integration harness that loads, resolves, relocates, seals, and executes fixture_add through cpu::execute."

[[tasks]]
id = "T002"
status = "implemented"
depends_on = ["T001"]
acceptance = "Provide bounded AAPCS registers/stack plus ARM-or-Thumb return sentinel handling and verify the exact fixture result without guest faults."

[[tasks]]
id = "T003"
status = "implemented"
depends_on = ["T002"]
acceptance = "Register the execution test in CTest and explicit Linux fixture evidence/artifact capture while keeping Android lanes build/probe-only."

[[tasks]]
id = "T004"
status = "active"
depends_on = ["T003"]
acceptance = "Pass exact-head required CI, reconcile accepted specs/state/evidence, and close the feature."
